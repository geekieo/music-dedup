# MusicDedup

本地重复音乐管理工具，支持千万级音乐库。

**纯 JavaScript 声纹识别**，无需安装任何外部工具。基于 Goertzel 频谱指纹算法，识别同一录音的不同格式与音质版本，结合智能保留规则自动决策。

---

## 快速开始

**环境要求：Node.js ≥ 18**

```bash
npm install
npm start
```

浏览器打开 **http://localhost:3456**

---

## 使用流程

1. **设置** — 点击"添加文件夹"，从系统对话框中选择音乐库目录
2. **扫描** — 开始扫描，实时查看进度，随时可暂停/继续/停止
3. **重复组** — 查看发现的重复组，确认删除方案
4. **确认** — 文件移入系统回收站，可撤销

---

## 核心功能

| 功能 | 说明 |
|------|------|
| 纯 JS 声纹识别 | Goertzel 频谱指纹，无需外部工具，识别同一录音的不同格式/音质 |
| 多阶段检测 | 精确指纹匹配 → 标题匹配 → 指纹前缀 LSH → 时长分桶，逐层扩大召回 |
| 智能保留 | 音质优先 → 首发专辑 → 单曲/专辑判断 → 元数据完整性 |
| 手动覆盖 | 点击任意曲目图标，覆盖自动建议 |
| 批量操作 | 一键确认所有待处理重复组 |
| 安全删除 | 移入系统回收站，不立即永久删除 |
| 实时进度 | SSE 推送扫描进度，多客户端同时可见 |
| 暂停 / 继续 / 停止 | 扫描中任意阶段都可暂停（保留进度，原地等待）或直接停止 |
| 自动保存 | 设置修改后 700ms 防抖自动保存；声纹阈值/时长容差/音质优先级的改动需要在"扫描"页手动点击智能执行才会重新匹配，避免后台静默触发与手动操作冲突 |

---

## 重复判断逻辑

### 识别阶段（4 阶段）

| 阶段 | 方法 | 说明 |
|------|------|------|
| 1 | 精确指纹匹配 | 指纹字符串完全相同 → 必定重复，无需阈值 |
| 2 | 标题匹配 | 从元数据或文件名提取歌名，归组后比对指纹 |
| 3 | 指纹前缀 LSH | 相同音频前缀整数相同，归组后比对全指纹 |
| 4 | 时长分桶 | 相同时长（±4s）归组，比对指纹 |

### 保留策略（优先级）

1. **音质最优** — Hi-Res FLAC > FLAC > WAV > M4A > MP3 320k > …
2. **首发专辑** — 年份最早的正式专辑（合辑不算）
3. **单曲 vs 专辑** — 本地专辑 ≥ 2 首保留专辑版；否则保留单曲版
4. **元数据完整性** — 标签最完整的文件

---

## 声纹算法说明

基于 **Goertzel 频谱指纹**（Sub-band Spectral Fingerprinting）：

1. 将音频解码为 PCM（支持 MP3、FLAC、M4A、OGG、WAV 等格式）
2. 重采样至 11025 Hz 单声道
3. 用 Goertzel 算法计算 33 个对数均匀分布频率（300 Hz–2 kHz）的能量
4. 比较相邻帧的频谱斜率变化方向 → 32 位整数
5. 120 个整数构成指纹（约 1 秒音频，快速且可靠）

相似度计算：对两个整数数组逐位做 Hamming 距离，即与 Chromaprint 相同的比较方法。

---

## 部署

### 本地（推荐）

```bash
npm install && npm start
# 打开 http://localhost:3456
```

### PM2 后台运行

```bash
npm i -g pm2
pm2 start server.js --name musicdedup
pm2 save && pm2 startup
```

### Docker

```dockerfile
FROM node:20-slim
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev
COPY . .
EXPOSE 3456
CMD ["node", "server.js"]
```

```bash
docker build -t musicdedup .
docker run -d -p 3456:3456 \
  -v /your/music:/music:ro \
  -v musicdedup-data:/app/data \
  musicdedup
```

### Nginx 反向代理（SSE 关键配置）

```nginx
location / {
    proxy_pass http://localhost:3456;
    proxy_http_version 1.1;
    proxy_buffering off;       # SSE 必须关闭缓冲
    proxy_cache off;
    proxy_set_header Connection '';
    chunked_transfer_encoding on;
}
```

---

## API

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/stats | 库统计 |
| GET | /api/settings | 获取设置 |
| PUT | /api/settings | 保存设置 |
| POST | /api/browse-folder | 弹出系统原生选择文件夹对话框，返回选中的绝对路径 |
| POST | /api/scan/start | 启动扫描（`steps` 数组决定运行哪些阶段，详见下方"扫描通道"） |
| POST | /api/scan/pause | 暂停当前扫描（在阶段边界处挂起，保留进度） |
| POST | /api/scan/resume | 恢复已暂停的扫描 |
| POST | /api/scan/abort | 停止扫描 |
| GET | /api/scan/stream | SSE 进度流 |
| GET | /api/duplicates | 重复组列表（`?resolved=0/1`） |
| GET | /api/duplicates/:id | 重复组详情 |
| POST | /api/duplicates/:id/resolve | 确认删除 |
| POST | /api/duplicates/resolve-all | 批量确认 |
| PUT | /api/duplicates/:id/tracks/:fid/keep | 手动覆盖 |

### 扫描通道（`/api/scan/start` 的 `steps`）

三条通道相互独立，互不触发对方的提取逻辑：

| 通道 | steps | 会做什么 |
|------|-------|----------|
| 基础匹配 | `enum, meta, match` | 枚举目录 → 元数据智能更新 → 仅按标题/艺术家/时长等元数据匹配 → 更新重复组。不触碰声纹、不触碰刮削 |
| 声纹匹配 | `enum, meta, fp, match` | 在基础匹配之上，智能提取声纹（按 `file_mtime` 跳过未变化文件）→ 重新匹配 |
| 刮削匹配 | `enum, meta, scrape, match` | 在基础匹配之上，向 MusicBrainz/AcoustID 智能刮削（仅刮削未刮削过的文件）→ 重新匹配 |
| 智能执行全部 | `enum, meta, fp, scrape, match` | 依次执行以上三条通道 |

声纹相似度阈值、时长容差、音质优先级的修改只影响 `match` 阶段本身，不会触发重新提取声纹——在"扫描"页对任意通道点智能执行（或在"设置"页点重新匹配提示条上的按钮）都只会重新比对，不重新提取。

---

## 项目结构

```
musicdedup/
├── server.js        # Express 入口 + 所有 API 路由
├── lib/
│   ├── db.js        # SQLite (WASM) schema + 查询
│   ├── fingerprint.js  # 纯 JS 声纹指纹
│   ├── scanner.js   # 文件扫描 + 元数据提取
│   ├── matcher.js   # 多阶段重复检测
│   └── rules.js     # 保留规则引擎
├── public/
│   ├── index.html   # 入口 HTML
│   └── app.js       # React SPA
├── data/            # SQLite 数据库（运行后自动创建）
└── README.md
```

---

## 性能参考

| 库规模 | 扫描耗时（16线程） |
|--------|------------------|
| 10 万首 | ~5 分钟 |
| 100 万首 | ~50 分钟 |
| 1000 万首 | ~8 小时 |

首次扫描后指纹缓存在 SQLite，增量扫描只处理新文件。

---

## 更新日志

### v1.7.4

**刮削逻辑修正**
- 刮削状态由三色改为四态判定：绿（精确匹配·完整）、蓝（精确匹配·有缺失，含繁简等价精确匹配）、红（模糊匹配）、无标记。
- 自动勾选逻辑与状态判定逻辑统一使用同一套"精确匹配"定义，修复模糊匹配下年份/专辑被错误标记"建议覆写"的问题。
- 音乐库刮削列支持点击排序（绿→蓝→红→无）。

**AcoustID / Chromaprint**
- 修复 `scrapeSingleFile` 仍使用内部 Goertzel 指纹查询 AcoustID 导致恒为 0 命中的问题，统一改用 Chromaprint。
- 声纹提取阶段补充：已有 Goertzel 指纹但缺 Chromaprint 的历史文件（例如后装 fpcalc 的情况）会自动补算。
- 设置页面 AcoustID 区块同时展示 API Key 与 fpcalc 检测状态，两者需同时满足才视为可用。

**写入与历史**
- 写入历史改为「最近写入」：以文件为单位，保留首次写入前的完整原始标签，多次写入不再产生多条记录，撤销恒定回到最初状态。
- 新增「剩余保留天数」列，默认 30 天后自动清理；新增"彻底删除"（需二次确认）。
- 移除"全部撤销"按钮；操作按钮（播放/查看/撤销/删除）改为横向排列。

**UI**
- 白名单"在音乐库中查看"改为真实跳转定位；移除"在重复组中查看"。
- 重复组保留/删除切换按钮改为中性图标，不再使用红色叉号。
- 扫描页面"高级"按钮与"智能执行"内联放置。

### v1.5.0

**播放器重构**
- 播放控件（上一曲/播放/下一曲）改为基于 `position:absolute + left:0+right:0+margin:auto` 的真正视口居中，不受左右面板宽度影响。
- 进度条改用 `window.addEventListener('mousemove/mouseup')` 全局指针捕获：拖动时鼠标可自由移出进度条区域，不会脱焦中断。
- 进度条 hover 时高度动态扩展（4px→8px），显示明显背景阴影；渲染可拖动圆形拖柄。
- 时间提示移到拖柄正上方，悬停/拖动时弹出圆角矩形 pill 显示"当前时间 / 总时长"。
- 音量喇叭图标（三态）始终可见；滑块平时宽度为 0，hover 音量区域后 CSS 过渡展开。
- 播放条为普通流元素（非 `position:fixed`），与页面自然衔接，不遮挡任何内容。

**音乐库重构**
- 统计栏改为三张交互筛选卡：**全部曲目 / 已刮削 / 重复文件**，点击后卡片高亮，下方列表即时切换，统计数字随之精确更新。
- 列表改为无限滚动（每页 80 条）；切换标签页再切回时恢复滚动位置。
- **已刮削**模式下每行末新增"应用"按钮（逐曲补全）；批量补全入口须滚到列表底部才激活，防误操作。
- 库级属性补全从设置页移除，合并到音乐库"已刮削"视图中。

**播放器定位修复**
- `playTrack` 记录来源（`src:'library'` / `src:'duplicates'`），点击播放条信息区正确跳转：重复组来源→重复组页，音乐库来源→音乐库并滚动定位到对应行。

**重复组存储统计修复**
- 按匹配类型/搜索词筛选后，顶部"X 组 · XX MB"基于当前可见的过滤结果，不再固定显示全量。

**设置页音乐目录**
- "更新音乐库"按钮不再常驻；仅在修改目录后显示动态提示横幅，点击后只枚举文件树，完成后横幅消失。

**运行日志高度**
- 日志高度改为 `calc(100vh - 520px)`，在更大窗口中自动拉高，不被播放器遮挡。

---

### v1.4.0

**刮削算法完全重写**
- 使用字符 bigram Dice 相似度 + 时长比对 + 曲目号比对 + 发行版脚本信号（简/繁）精确选择候选，结果带 `match_basis`：`exact`（有佐证）或 `fuzzy`（最后兜底）。
- Phase 2c（MB 确认分组）仅信任 `exact` 匹配，`fuzzy` 不影响分组。
- 刮削数据不再做繁→简转换，原始文本直接存储。

**播放器 UI（v1.3.0 → v1.4.0）**
- 进度条移至播放条顶部，音量喇叭图标始终显示，音量滑块 hover 展开，封面缩略图按需请求。

**音乐目录**
- 手动路径与"选择文件夹"按钮合并为同一行；失去焦点即保存；添加目录后默认只执行枚举。

**库级属性补全（初版）**
- `/api/library/smart-fill`：精确匹配可覆写错误专辑/年份；模糊匹配只填空字段；标题/艺术家从不覆写。

---

### v1.3.0

- 修复智能扫描核心 bug：`scan_time` 在每次枚举时都会被刷新，导致元数据/声纹的"智能跳过"判断永远判定为"需要更新"，声纹提取因此每次都变成全量重新提取。现在改为只比较 `file_mtime`，未改动的文件不会再被重复处理。
- 设置页修改声纹相似度 / 时长容差 / 音质优先级后不再自动触发后台重新匹配（避免与"扫描"页的手动操作冲突、产生"已有扫描进行中"的误报）；改为页面上的一条提示横幅 + 手动按钮，点击后只重新比对，不重新提取声纹/不重新刮削。
- 扫描新增暂停 / 继续，配合已有的停止，三者在扫描的任意阶段边界都可用。
- 重复组页面切换标签页不再重新加载（之前每次切回都会触发新一轮请求和加载动画）；播放器状态从全局对象中拆分，避免播放进度刷新拖慢列表页渲染。
- 添加扫描目录改为弹出系统原生文件夹选择对话框，不再需要手动输入路径（仍保留手动输入作为备用入口）。
- 音乐库列表新增时长列；音质优先级列表新增"恢复默认"，行高收紧。
- 播放器组件改为整体居中并设上限宽度，新增音量与上一首/下一首。
- 页头与导航合并为一行：左侧品牌、导航居中、状态靠右。
- 新图标：两个交叠的音符，前实后虚，呼应"去重"。


